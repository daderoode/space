// dialoguer removed in v0.2.0 — stub until TUI rewrite (Task 10/11)
use anyhow::Result;
pub fn run(_repo_args: Vec<String>) -> Result<()> {
    anyhow::bail!("not yet implemented in v0.2.0 — use TUI")
}
